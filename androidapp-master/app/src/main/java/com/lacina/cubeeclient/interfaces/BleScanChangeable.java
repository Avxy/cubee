package com.lacina.cubeeclient.interfaces;

/*
*   Implements a method to tell to scan fragment which activity to go when scan got a result
* */
public interface BleScanChangeable {
    Class activityToGoTo();
}
